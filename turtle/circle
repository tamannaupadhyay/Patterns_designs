from turtle import *
from math import sin,cos,pi
r=50
inc=2*pi/50
t=0;n=1
for i in range(100):
    x1=r*sin(t);
    y1=r*cos(t);
    x2=r*sin(t+n);
    y2=r*cos(t+n);
    penup();  goto(x1,y1)
    pendown();goto(x2,y2)
    t+=inc
#circle made of straight lines
